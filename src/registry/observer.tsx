import { observer } from "mobx-react"
import * as React from "react"
import { BaseComponent, BaseComponentProps } from "./base"

@observer
export class ObserverComponent extends BaseComponent<BaseComponentProps> { /* */ }
